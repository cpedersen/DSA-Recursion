// What is the input to the program?: x, y, path, maze

// What is the output of the program?: array of strings, with each string
// containing characters indicating Left (L), Right (R), Up (U), Down (D).

// What is the input to each recursive call?: navigateMaze(x, y, path, maze)

// What is the output of each recursive call?:
// return.concat(navigateMaze(x, y, path, maze))

const allWaysOutMaze = function () {};

console.log(allWaysOutMaze());
